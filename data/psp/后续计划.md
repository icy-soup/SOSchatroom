# PSP 游戏「凉宫春日的约定」数据 — 后续计划

## 当前状态

已从 `cvn-smhny_V1.1.iso`（CVN汉化版）提取出：
- **对话 11,285 条**（`psp_dialogues.json` / `.txt`）— 带 speaker 标签的中文对话文本
- **场景样例**（`psp_full_scenes.txt`）— 尝试提取了旁白，但效果不佳

## 格式分析

PSP 脚本文件（`script/*.dat`）采用自定义二进制格式：
- `kyo001010010` → `角色代码 + 场景/段号`
- 对话在 `「」` 内，GBK 编码 ✓ 已提取
- 旁白由 `w` 前缀标记（`wekyo...`），但二进制控制字节干扰解析 ✗
- 角色表情/动作编码在二进制状态字段中，未解析 ✗
- 选项分支数据可能嵌入在控制结构中 ✗

## 后续可以做的事

### 1. 完整解析脚本格式
- 逆向工程 `.dat` 文件的二进制结构
- 识别：控制字节（角色状态/表情/位置/时间）
- 提取旁白文本（`w` 前缀段）
- 提取选项分支结构
- 参考：PSP 游戏常用 Binary Structured Text 格式

### 2. 重建场景连贯性
- 按路线合并场景（`s_har*` = 春日线、`s_nag*` = 长门线等）
- 还原选项分支流程
- 匹配对话与角色表情状态

### 3. 多数据源融合
- PSP 对话 + 小说全文13卷 → 更完整的交互统计
- 小说提供叙事上下文，PSP 提供结构化角色标签
- 对照验证概率计算的准确性

## 注意

游戏是衍生作品，对话经过改编，与原著小说不完全一致。
小说原文（`reference/1-11凉宫春日物语.txt` + 12/13卷）仍是概率计算的主要数据源。
PSP 数据更适合作为补充验证和角色互动丰富度的参考。
